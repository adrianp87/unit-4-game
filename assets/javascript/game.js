let randomNumberGenerator = Math.floor((Math.random() * 1000))



// On click, generate random number


// Adds points to player's total

// Only shows amount when crystal is clicked
// Then updates score

// If total score matches random number, player wins

// player loses if score goes above random number

// game restarts if player loses

// new values given to crystals. Must reset counter to zero


// keep tally of games won/lost